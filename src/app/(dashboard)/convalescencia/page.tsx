'use client';

// Duplicacao temporaria de src/app/page.tsx (UI convalescenca, ~linhas 3007-3075 + modal ~4539-4656).
// Resolvida quando o monolito for removido na Fase 6.

import React from 'react';
import ConvalescenceTab from '@/components/tabs/ConvalescenceTab';

export default function ConvalescenciaPage() {
  return <ConvalescenceTab />;
}
