// Duplicacao temporaria da fase 5c-2 (API orfa — UI nova, sem codigo
// em page.tsx para copiar). Resolvida quando o monolito for removido
// na Fase 6.

import AuditLogsTab from "@/components/tabs/AuditLogsTab";

export default function AuditoriaPage() {
  return <AuditLogsTab />;
}