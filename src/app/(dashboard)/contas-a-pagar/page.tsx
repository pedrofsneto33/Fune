// Duplicacao temporaria da fase 5c-1 (API orfa — UI nova, sem codigo
// em page.tsx para copiar). Resolvida quando o monolito for removido
// na Fase 6.

import AccountsPayableTab from "@/components/tabs/AccountsPayableTab";

export default function ContasAPagarPage() {
  return <AccountsPayableTab />;
}