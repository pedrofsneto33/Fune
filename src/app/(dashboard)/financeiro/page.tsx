// Duplicacao temporaria da fase 5a-1 (componente extraido da aba Financeiro
// de page.tsx, versao READ-ONLY para validar leitura antes das mutacoes).
// Resolvida quando o monolito for removido na Fase 6.

import BillingTab from "@/components/tabs/BillingTab";

export default function FinanceiroPage() {
  return <BillingTab />;
}
