// Fase 4b — painel admin SaaS (superadmin global). Espelha o padrao de
// auditoria/page.tsx: wrapper fino, sem logica. O componente e self-contained.

import { SaasAdminTab } from '@/components/tabs/SaasAdminTab';

export default function SaasAdminPage() {
  return <SaasAdminTab />;
}
